import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // PWA support is configured manually with app metadata and public/sw.js.
  // This project intentionally does not use next-pwa in Next.js 16.
};

export default nextConfig;
